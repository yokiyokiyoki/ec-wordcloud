const myLib = (text: string): string => {
  return text;
};

class EWordcloud {
  constructor(el: HTMLElement) {
    this.$el = el;
  }
  /**
   * setOption
   */
  public setOption(option) {}
  /**
   * resize
   */
  public resize() {}

  /**
   * showLoading
   */
  public showLoading() {}
  /**
   * hideLoading
   */
  public hideLoading() {}
}

export default myLib;
