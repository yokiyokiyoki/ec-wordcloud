const myLib = (text: string): string => {
  return text;
};

class EWordcloud {
  constructor(el: HTMLElement) {
    this.$el = el;
  }
  /**
   * setOption
   */
  public setOption(option) {}
}

export default myLib;
